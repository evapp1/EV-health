# App and bootstrap boundary

This directory owns application composition only: the root widget, future
environment configuration, dependency injection, navigation, and theme setup.
It may select concrete infrastructure implementations but must not contain
business rules.
