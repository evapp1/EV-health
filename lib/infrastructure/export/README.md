# Report generation

PDF and image renderers, app-controlled export storage, and sharing adapters
belong here. Renderers consume immutable report models and never depend on a
Flutter widget tree or live providers.
